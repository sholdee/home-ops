apiVersion: v1
kind: Service
metadata:
  name: zwaveui-proxy
  namespace: hass
  labels:
    app: zwaveui-proxy
spec:
  selector:
    app: zwaveui-proxy
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
    name: http
  type: ClusterIP
  clusterIP: None