apiVersion: apps/v1
kind: Deployment
metadata:
  name: portainer
  namespace: hass
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: portainer
  template:
    metadata:
      labels:
        app: portainer
    spec:
      containers:
      - name: portainer
        image: portainer/portainer-ce:2.21.4
        ports:
        - containerPort: 9443
          name: https
          protocol: TCP
        - containerPort: 9000
          name: http
          protocol: TCP
        volumeMounts:
        - name: portainer-data
          mountPath: /data
        resources:
          requests:
            memory: 100Mi
            cpu: "5m"
          limits:
            memory: 200Mi
        livenessProbe: &probe
          httpGet:
            path: /
            port: https
            scheme: HTTPS
        readinessProbe: *probe
      volumes:
      - name: portainer-data
        persistentVolumeClaim:
          claimName: portainer-pvc
