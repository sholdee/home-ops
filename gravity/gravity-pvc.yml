apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gravity-pvc
  namespace: gravity
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi